#include "filter_comment_body_regexp.hpp"


const char* regexpr_str = 
    "(?:^|[^a-zA-Z0-9_])(?:"
#define GROUP_UNCONDITIONAL_DEF 1
        "("
            "libcompsky"
        ")|"
        
#define GROUP_PROGRAMMING_DEF (GROUP_UNCONDITIONAL_DEF + 1)
        "("
            "[cC]++|"
            "[pP]rogramming|"
            "(source|header) files?"
        ")|"
        
#define GROUP_LINUX_DEF (GROUP_PROGRAMMING_DEF + 1)
        "("
            "[lL]inux|"
            "[uU]buntu|"
            "[oO]pen.?[sS]use"
        ")"
    ")s?[^a-zA-Z0-9]|"
    
#define GROUP_EGG_DEF (GROUP_LINUX_DEF + 1)
    "("
        "dQw4w9WgXcQ|"
        "YxmdCxX9dMk"
    ")"
;

#ifndef _WIN32
static_assert(GROUP_UNCONDITIONAL_DEF == GROUP_UNCONDITIONAL);
static_assert(GROUP_PROGRAMMING_DEF == GROUP_PROGRAMMING);
static_assert(GROUP_LINUX_DEF == GROUP_LINUX);
static_assert(GROUP_EGG_DEF == GROUP_EGG);
#endif
